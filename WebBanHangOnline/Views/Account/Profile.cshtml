@model WebBanHangOnline.Models.CreateAccountViewModel
@{
    ViewBag.Title = "Hồ Sơ";
}
<link rel="stylesheet" type="text/css" href="~/Content/assets/styles/categories_styles.css">
<link rel="stylesheet" type="text/css" href="~/Content/assets/styles/categories_responsive.css">

<div class="container news_section_container">
    <div class="row">
        <div class="col-md-12">
            @using (Html.BeginForm("PostProfile", "Account"))
            {
                @Html.ValidationSummary(false)
                @Html.AntiForgeryToken()

                <!-- Include phần UserInfo -->
                @Html.Partial("UserInfo", Model)

                <!-- Include phần UserOrders -->
                @Html.Partial("UserOrders", Model.UserOrders)

            <button type="submit" class="btn btn-success">Cập Nhật</button>
            }
        </div>
    </div>
</div>
